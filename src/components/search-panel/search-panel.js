import './search-panel.css'

const SearchPanel = () => {
    return (
        <input
        type='text'
        className="form-control"
        placeholder="Find an employee"
        />
    )
}

export default SearchPanel;